# Project continuity

15 September SERVO-POWER-HARNESS-01 v0.1: cad/servo-power/README.md/viewer.html is latest nominal wiring. Two proposed OD1.6 wires from upper existing P2.5VOUT/P2.2GND tails, topZ39.1=29.6-1.5+11 in approximate layout (not terminal top40.8). Lengths133.028/137.588mm are not cut lengths. Pin groups reused, physical numbering/height and servo endpoints unverified. Existing lower logic pair and D3 signal unchanged. Two local1.9x1.9x1.8 contact windows only; full buck restored for service. Fit/pair/cavity clear;170 fixed bodies/265 body/25 guard/continuous50mm battery exit pass at0/+3. Shortcuts hit actual obstacles. Old servo-bundle/old-clash removed from new scene, two actual wire reserves added;76 objects/WebGL0/canvas inspected. No connector/strain relief/bends/current/RF/off-state/calibration proof. Motor terminal/input-power conflicts and charging/protection remain. Printing deferred; SG90 variant question pending, do not repeat.

15 September SERVO-WIRING-01 v0.1: cad/servo-wiring/README.md/viewer.html is latest nominal wiring. D3 U9.4/GPIO4 extracted from pinned Seeed PCB, existing D0/D1/GND transform and firmware variant cross-checked. OD0.8 signal goes below expansion71; straight exit counterexample intersects it0.599999mm3. All103 XIAO boxes whole, only contact-end wire clipped atY83.25. Length59.219mm not cut length, servo endpoint approximate. Geometry/service169 fixed bodies/265 body poses/25 guard/continuous50mm battery exit pass at0/+3. Old bundle display STL is not volume on reload; service rebuilds original points/radius, no silent repair. Proposed power P2.5VOUT/P2.2GND separate branch; not routed, input3.3V/off-state/backfeed/current/calibration unqualified. TowerPro Digital0.5..2A is not the ordered SG90 rating. Motor terminal/input-power conflicts remain, printing deferred. Exact SG90 variant question remains pending; do not repeat.

15 September user-confirmed Samsung unlock rechecked: tools/run_video_android_smoke.py passed7/7 on RF8M811X8FK API31, same APK hashes as control-identity-v01. Evidence docs/evidence/video-samsung-unlocked-recheck. No rebuild, RF, real camera, firmware flash or actuator test. Unlock is resolved; do not ask again without fresh contrary evidence.

15 September SERVO-BUNDLE-01 v0.1: cad/servo-bundle-study/README.md/viewer.html is the latest nominal wiring scene. Old OD1.8 servo reserve intersected C2 by2.471619mm3; new OD3.3 reserve clears checked solids/cavity, service168 fixed bodies/265 body poses/25 guard poses/continuous50mm battery exit passes at0/+3 only. Assumes three OD1.4 loose wires, centre spacing1.6, not actual SG90 ribbon. Both ends are packaging waypoints, not pads; power/D3/connector/bends/strain relief unassigned. Length55.505mm is not cut length. Old motor-terminal-A and power corridor conflicts remain. Viewer75 unique objects/WebGL0; screenshot timed out. SG90 exact variant link question pending; do not repeat. Printing deferred.

15 September CONTROL-IDENTITY-01 v0.1: docs/control-identity.md. Discovery selection retains full chip ID; Android requires type130 IDENTITY then matching-token HELLO on /control/identity before ARM, within original4s. Wrong/missing/repeated/late identity or token mismatch rejects; no automatic legacy fallback. Actual manual address edit clears ID and uses legacy /control, labelled in UI. Selected ID now joins video key. Firmware a9db977 sends actual ESP.getEfuseMac prefix only on exact path; WebSockets2.6.1 cUrl pointer/length checked. Final47 JVM pass with native bridge/no skips;7 Samsung UI tests pass, with fixture picker callback replacing only UDP source. Same-text replacement does not trigger editing; manual test clears/re-enters address. Four firmware builds/host checks/SDK syntax probes pass; not flashed, no RF/physical ID/actuator proof. Older full opt-in ELF is historical. Canonical control-v1 copies kept identical; secure pairing/camera identity still open. Printing deferred.

15 September FLEET-CONTROL-01 v0.1: docs/firmware-fleet-bench.md and tools/run_firmware_fleet.py. Host-only six JVM CarControlClient instances with real firmware control.h/codec via C++ stdin bridge and MockWebServer. Six intruders rejected, replacement neutral/new token, one silent link expires by native tick while other five sequences advance. Copied-header removal of active-owner guard detected by exact Second connection stole car assertion. Firmware/main app unchanged; no ESP32 main/RTOS/HAL/RF/video/USB. One compound test, not six physical cars/load proof. Normal Gradle skips this test without RC_CONTROL_BRIDGE; runner compiles baseline/mutation and uses --rerun-tasks. Evidence/firmware-fleet-v01 stores actual source/binary/JUnit hashes. Existing37 JVM/5 Samsung UI reports remain earlier distinct evidence. Printing deferred.

15 September CAR-VIDEO-01 v0.1: docs/car-video-selection.md. CarControlScreen keys video lifecycle by trimmed host/controlPort; target change closes old view/stream, clears frame and resets URL to same-host81/stream. Manual camera URL remains, no auto start/ARM; FAIL-01 and same-target mode/STOP behavior unchanged. New Samsung test blue->black->red plus control passed;37 JVM/5 Android tests, runner now expects5. Intermediate black-pixel failure was Samsung IME overlay ff2d2d2d, screenshot/ime=true confirmed; test hides IME before inspecting pixels. Endpoint association only, not chip-ID/auth/DHCP proof or six-car/Discovery picker UI qualification. Evidence/car-video-selection-v01; earlier4-test APK results historical. Printing deferred.

15 September CONTROL-HELLO-01 v0.1: docs/control-hello-deadline.md. Android now checks initial HELLO age>=4000ms in receive handler before token/ARM, closing timer race. 3999/4000/4001ms tests force receive before timer; two failed before fix,37 JVM tests pass after. Fresh APK bfb9cf2c installed on Samsung RF8M811X8FK; four loopback UI tests pass, evidence/control-hello-deadline-v01. Earlier Samsung-v02 APK evidence historical; no RF/camera/actuator proof. Deadlines/protocol unchanged; printing remains deferred.

15 September HIGH-SIDE-01 v0.1: docs/high-side-protection.md screens BQ28Z610-R1, not selected. High-side 1-2S protector/gauge/balancer, VSON12 4x2.5. ASCC minimum magnitude22.2mV yields2.22A even at10mohm; does not independently catch charger1.5A reset. OCC/configuration/swap wake/shunt-ground paths unqualified. TI PMP40496 uses older BQ28Z610 plus BQ25883, not a ready LW charger. R1 ChipDip8017659994 costs790RUB/MOQ1,6-7weeks; cheaper300RUB search result lacksR1. A/B BOM unchanged; no purchase/full circuit/hardware proof. Samsung unlock already resolved and4/4 tests passed; printing remains deferred.

15 September POWER-GROUND-01 v0.1: docs/power-ground-domains.md and tools/check_power_ground_domains.py. Ideal low-side return cutoff can be bypassed if nonisolated charger ground is RAW_N and service XIAO ground is protected, via two USB cables to common host/hub; positive drive OFF does not break grounds. Direct raw-monitor ground to DRIVE_GND is another counterexample. Current carrier netlist H3/H5/H10/U1.3 verified common. 32 proposed wiring states and16 miswire states checked; graph omits cells, MID/CBSET, semiconductor/ESD/shield paths and transients. Proposed external protected ground bus is necessary wiring constraint, not qualification of BQ25887/BMS or charge/discharge cutoff. TI RevB MID9/CBSET10 functions rechecked; local PDF fetch403, no fabricated download. Charger/protection scheme and physical input harness still open. Optional delivery-order/ETA question asked; do not repeat order-status or initiate printing.

15 September DRIVE-POWER-CARRIER-01 v0.3 / XIAO-RETURN-H10-01 v0.1: cad/xiao-return-h10/README.md is current wiring. Added H10 DRIVE_GND at PCB18/1.7, car27.45/65, underside29.5; GND0 moves from H5 to H10. H5 is traction input return; H3 buck output ground. Same electrical ground, no isolation. Native ERC/DRC/parity0,26 positions/58 pins/seven mutations. All57 previous combined-pad geometries and tracks match pinned957e015; zones refilled. Named-instance footprint assignment replaces unsafe first-string replacement. New OD1.2 return beneath PCB clears and service passes at0/+3. Input power path still absent; inherited terminal A and old input corridor conflicts remain open. Old v02/source hashes historical at957e015; current reports v03 and xiao-return-h10-v01. Old harness generators produce historical scenes; use current README command order. No fabrication/print/flash/physical qualification.

15 September MOTOR-HARNESS-01 v0.1: cad/motor-harness/README.md. H6/FO and H7/BO now have separate OD1.6 routes to estimated upper terminal faces; old wire-motor removed from scene. Wire fit passes, COMPLETE FIT FALSE: terminal A reserve overlaps closed upstream side piece246 by3.124286mm3. A/B reserves4.4x3x3, Y/Z offsets estimated; only rear4+/-0.4 comes from refreshed matching F130 PDF. Do not shrink reserve or notch suspension to claim fit before sample. Print-source tray2 part1 and tray1 part5 rigidly match ref246/281 with bidirectional vertex-surface max<.000013mm; source solids pinned with GPL notice. 13 nearby mechanics checked locally; service168 prior/new bodies passes265/25/50, not full moving suspension. Source wire lengths45.654/66.343 are not cut lengths. Input corridor still intersects tray0.333mm3; motor terminal measurements needed on arrival. No physical tests/print/flash. Viewer74/WebGL0. Generator exit0 is report generation, not complete-fit pass.

15 September DIRECT-LOGIC-HARNESS-01 v0.1: cad/direct-logic-harness/README.md is current proposed logic output routing. Soldered P2 tails to H8/H3, OD1.6; native header retained, mating plug removed. Tray saddle with OD0.8 cord and 3x3x2 knot reserve, no selected cable tie. New geometry clears at0/+3; service165 fixed bodies/body265/guard25/continuous battery50 passes. Old approximate wire-power/motor already intersect old tray0.333/3.633mm3; replace with actual endpoint routes next. Added saddle clear does not mean whole assembly fit. Tail geometry, solder/insulation, bends/current, cord/print strength and carrier extraction unqualified. SM optional; battery quick disconnect retained. Viewer71/WebGL0. Samsung4/4 already passed. Printing deferred.

15 September LOGIC-CONNECTOR-01 v0.1: cad/logic-connector-study/README.md. Inline SM candidate, not native2.54 header mate; proposed socket contacts on5V source, pin1 WAVE_5V/pin2 DRIVE_GND. JST catalog envelope22.9x8x8.7 enlarged23.4x8.5x9.2; four transverse poses fit, two longitudinal collide input terminal47.685/73.695mm3. Old buck plug/output pair removed from study; no new wiring/service/mount/thermal proof. ChipDip Scondar SCT2505FH-02PL100 HK0083-0005 86RUB is ONE HALF only,892 stock, Cheboksary18Sep estimate. DrawingA2022-09-30:24AWG,11x.16,OD1.4,100±5mm,pin1red/2black,MALE metal contacts. JST fit does not qualify Scondar body; complete ready pair not found. CONN-LOGIC-01 CSV costs only this half. Crimp-tool question pending; default ready leads until answered. Print deferred; no orders/hardware changes.

15 September BUCK-OUTPUT-HARNESS-01 v0.1: cad/buck-output-harness/README.md/viewer.html continues the XIAO scene. Manufacturer schematic freshly downloaded matches earlier PDF SHA6732db15; P1 pins1/2GND,3/4VIN; P2 pins1..3GND,4..6VOUT. Physical groups from top-view polarity; exact pin numbering within triple inferred. Proposed OD1.2 wires from estimated mating housing topZ43.3 at rear Y48.1 to H8/H3, replacing H8 dummy lead. Fit clear; service164 fixed bodies/body265/guard25/continuous battery50 passes; too-high GND positive collision12.553mm3. Viewer70 objects. Connector/retention/key/current/bends not selected or qualified; no full battery/protection/charger integration or physical wiring. Printing deferred. This closes nominal output-pair routing only, not strain relief or physical prototype.

15 September XIAO-POWER-HARNESS-01 v0.1: read cad/xiao-power-harness/README.md. Cached vendor rear pads BAT0.1/GND0.1 feed proposed H9/H5 on combined carrier v0.2; removes old thin U9.13-to-H3 GND, retains FI/BI. Proposed OD1.2 corridors, flat end at rear Y82; all103 XIAO boxes unmodified. Separate cradle variant has5.2x2.8 window; old cradle positive collision1.404mm3 per wire, new fit clear. Service with163 fixed solids: body265/guard25 samples and continuous50mm battery-box exit pass. Only0/+3, no physical wiring, strain relief, bend radius, full electrical/thermal qualification or camera/carrier extraction. Waveshare input pair still absent; never2S-to-BAT0. Viewer69 objects, browser WebGL0. Printing remains deferred. Samsung4/4 proof below remains current evidence; do not repeat resolved unlock question.

15 September Samsung unlock resolved for this run: user unlocked RF8M811X8FK, fresh adb policy showing=false/awake; tools/run_video_android_smoke.py passed4/4 on SM-G973F Android12/API31. docs/evidence/video-samsung-v02 stores instrumentation, latest scoped logcat PID25743 at18:14:35..52 and APK/source provenance. Pixel cycleff0000fe->ff000000->ff0000fe confirms physical phone composition. APK hashes identical to prior API35 run, app tree unchanged since275f13f; no rebuild. Existing720x1520/320dpi display overrides observed, not changed. Loopback video/control only; no real camera/RF/actuator/firmware action. Old Samsung-locked notes are historical, not a current standing blocker; recheck device state before future UI work without repeating the resolved question. Printing remains deferred. Power-wire CAD was paused for the user-enabled phone test, no harness edits yet.

15 September DRIVE-POWER-CARRIER-01 v0.2: read hardware/drive-power-carrier/max40200-package.md. Official ADI21-0057 RevK includes U5+1, D/Emax3mm,Amax1.45; conservative reserve3.5x3.5x1.5 includes .25 protrusion each side. Maxim90-0174 RevB drawing read through AllDatasheet browser image: pads1.30x.55 rectangle, row centers2.50,pitch.95. Current manufacturer land revision not confirmed. Corrected U3 footprint, U3+0.1X/C5-0.1Y/H9+0.2X and routing; original motor positions retained. Native ERC/DRC/parity0 and new nominal fit clear; positive control18.375mm3. Reports v02 current, v01 historical c041b2b. PIN2571C official PDF is SHIPPING BOX change only, no fit/form/function impact; not electrical errata. Physical/process/thermal/new wire/service checks still open; print deferred.

15 September DRIVE-POWER-CARRIER-01 v0.1: hardware/drive-power-carrier and cad/drive-power-carrier propose combining TA6586 with XIAO-LOGIC-01 v0.2 on the existing 30x18x1.6 PCB. Original 27 pad geometries/14 positions retained, two VM track segments detoured; add bottom TPS737 U2/MAX40200 U3 and H8 WAVE_5V/H9 XIAO_BAT. 25 positions/57 pins/12 working nets, ERC/DRC/parity0, six independent pin-oracle mutations detected. Nominal0/+3 new-part fit: 11 reserves against156 obstacles clear, positive control15.36mm3. Lowest reserveZ28 vs old28.6, upper44.6 unchanged. H8/H9 routing and new service/adjustment paths remain open. SOT23 footprint/envelope provisional pending ADI21-0057/90-0174 and PCN2571C; .15mm rules, thermal vias/paste and shared LDO/motor heat not qualified. This is a separate candidate, not a released PCB, complete charger or physical power proof. Printing explicitly remains deferred; do not ask for printer IP/material or start printing.

15 September XIAO-LOGIC-01 v0.2 supersedes v0.1: hardware/xiao-logic-power now uses MAX40200AUK+T SOT23-5 after TPS73701. Remove J4 and R4; U2 EN locally tied VDD, internal reverse blocking, no service-VBUS wire. C3/C4 now1uF, each effective>=.33uF and total OUT load<=100uF. 13 positions/32 pins/5 nets and ERC0; 320 conditional DC corners. SOT23 maxdrop175mV at500mA/3.3V, not WLP43mV; extension to4V remains assumed. Same LDO heat and unqualified transitions/PCB/physical path. ChipDip50RUB/MOQ1 observed15Sep, not purchased; PCN2571C listed by ADI but contents unread. v01 reports remain historical in commit11380ef. Separate2S charging/protection unfinished; printing deferred. PowerShell piping Cyrillic to Python needs explicit UTF8 OutputEncoding; use apply_patch for Russian docs to avoid question-mark corruption.

15 September DRIVE-INTEGRATION-01 v0.2: firmware checker commit f9f582d links four opt-in ELF/bin images from runtime a8c25f6 in D:/RaceRemote-build-checks/actuator-link-wr66dxap. Source snapshot excludes local secrets; copied library caches/build outputs are isolated. Both motor/servo HALs and setup/watchdog are linked; XIAO images also include camera service/driver. Eight default artifact hashes and 20 runtime source hashes unchanged. docs/drive-integration-link-evidence.json in firmware records the proof. Synthetic calibration images are marked DO-NOT-FLASH, not uploaded; no physical GPIO/current/timing/calibration evidence. The v0.1 linking gap is closed; hardware qualification, power/charging and physical build remain open. Print remains deferred.

15 September DRIVE-INTEGRATION-01 v0.1: sibling docs/drive-integration.md. main has a separate RC_ENABLE_ACTUATORS=1 path with mandatory calibration; no bundled environment enables it. Pair initialization precedes network/camera; output fault/rejected/stale plan revokes session/token and zeroes public plan under the watchdog mutex, with a persistent connection interlock. Normal expiry permits a new explicit ARM; camera remains independent. Default four images stay null; host checks/eight profile cases/two real-SDK syntax-only probes passed. No opt-in linked image, upload, physical outputs/calibration or stopping proof. outputsEnabled denotes selected hardware backend, not physical feedback; acceptingControl conveys inhibition. Earlier no-main-integration notes are historical. Printing remains deferred and parts unreceived.

15 September TA-USB-STATIC-01 v0.1: docs/ta6586-usb-fixture.md adds a USB-only manual TA6586 fixture proposal: XIAO VBUS through47ohm/5%/2W, four1kohm output loads, 3V3 jumpers, no GPIO/LW/motor/servo/charger. VM must be measured >=3.5V with VBUS4.75..5.25 and logic3.15..3.45; failure means manual disconnect, not automatic protection. 512 ideal DC corners/KCL/power balance passed; assumed ICC0..7mA is not qualified below6V. Series resistor does not limit downstream capacitor discharge or upstream faults. No assembly/purchases/physical measurements; XIAO remains in transit and printing deferred. This fixture is a diagnostic step, not completion of 2S drive or built-in USB-C charging.

15 September emulator local cleanup: all owned emulator processes stopped, but D:/raceremote-video-dr80xg66 remains. Automatic review rejected recursive removal with Force and then without Force; do not bypass this rejection via another deletion tool. Existing user AVD directories were not deleted. Ignored build/video-emulator/temp-avd.json records the reusable temporary API35 AVD home/name (RaceRemoteVideo_API35); final viewport1280x720/density320. C free space was only~0.23GB at end, recheck before large builds/downloads and keep future emulator data off C. This is an environment limitation, not proof the physical prototype is blocked in every direction.

15 September VIDEO-METADATA emulator validation: docs/evidence/video-emulator-v02/summary.json, docs/video-freshness.md. Four UI tests passed on temporary API35 at1280x720/density320, including system screenshot blue->black->blue, metadata reconnect with reset clock, continued control, gestures/background/STOP. No new Samsung/physical camera/RF/FPS/latency proof. API36 first run had ACK215ms before video, so field setup moved before ARM; 200ms limit unchanged. Pixel assertions distinguish dimmed blue from black, not brightness. Tablet layout/portrait override attempts failed; only final landscape viewport qualified. tools/run_video_android_smoke.py requires explicit serial, unlocked API31+, and OK(4 tests), not adb exit0 alone. Test cleanup always closes servers on UI failure. User print remains deferred; pending Samsung unlock question must not be repeated unnecessarily.

15 September VIDEO-METADATA-01 v0.2: docs/video-freshness.md. Android now consumes camera X-Sequence(uint32) and X-Timestamp(seconds.six-microsecond-digits). Per connection, reject replay/backwards/repeated capture before decode; rejected parts do not renew view freshness timeout. Sequence wrap/gaps allowed via unsigned half-range; metadata presence cannot change within session. Generic no-metadata MJPEG remains supported without capture-freshness proof. UI/log counter is accepted, not received; no camera-phone clock subtraction or backlog-age claim. Host checks/builds pass; new on-phone repeated-capture/reconnect scenario not yet passed because Samsung remained keyguard-locked. Do not rerun phone UI tests unchanged while locked. User was asked once to unlock when convenient; printing still deferred. Firmware unchanged/null outputs.

15 September CURRENT-SERVICE-01 v0.1: cad/current-service/README.md/viewer.html. New carrier, mount, antenna and three control leads rechecked at nominal0/+3: 152 fixed solids, body265/left-guard25 samples at.25mm and continuous50mm battery-box exit clear. Body path +2Z,+1X,then +65Z; positive controls retained left guard123mm3 and straight-lift USB14.821mm3 detected. Control leads remain fixed during body removal; this does not qualify carrier/camera extraction with wires. Battery tail/connectors, real shell/fingers/magnets/force, some suspension parts, charger and other adjustment poses remain open. Print remains explicitly deferred. Viewer animates checked body/lid positions only.

15 September MOTOR-PRINT-KIT-01 v0.1: user explicitly postponed actual printing; do not ask again for printer IP/loaded filament now or start a print. cad/motor-carrier-mount/print-kit.3mf packages three existing print meshes (tray/clamp/PCB gauge), millimetres, floor Z=0, 10mm XY bounding-box gaps on K1C bed. Independent trimesh import preserves three closed bodies, bounds, triangle counts and volume. No slicer import, layer preview, G-code or physical print evidence; CrealityPrint7 --help only produced startup logs. No filament/nozzle/process profile embedded. Offline software/CAD/circuit work remains possible while ordered parts travel.

15 September CONTROL-HARNESS-01 v0.1: cad/control-harness/README.md. Three separate .8mm proposed lead corridors D0/GPIO1->H1, D1/GPIO2->H2, GND->H3; firmware remains unused/null. Vendor KiCad URL says1.4 but archive contains Sense1.5; extract U9 PTH1/2/13, cross-check USB/U.FL placement in STEP2023, not received-board identity. D0/D1 are lower edge, GND upper; target GND .25mm inward. Current routes above driver clear named solids, pairwise wires, antenna/serviceUSB/cavity; local contact exceptions only PCB37 windows. Lengths77.764/81.604/66.370mm are centreline paths, not cut lengths. Strain relief, detachable connector, actual wire SKU/bends/levels/domains and service/adjustments with wires remain open. Do not transfer previous freed-wire PCB extraction proof. Under-board path failed base/USB; direct shortcut is collision positive control.

15 September MOTOR-MOUNT-01 v0.1: cad/motor-carrier-mount/README.md and motor-carrier-mount.scad. New tray integrates edge supports/right fixed lip; removable left clamp uses two proposed M2x6/nuts. PCB top/side clearances .3mm, nut entry5.1mm across corners; install nuts before adjacent buck/PCB. Nominal0/+3 solid/cavity/gauge/key-access checks and sampled PCB171/clamp81/nut25 service poses pass; straight-up PCB positive control collides. PCB extraction requires tilt6deg,left1.4,up8,rear8,then up; old body-removal/adjustment evidence does not transfer. Print-oriented tray/clamp/dummy PCB STLs prepared, no print/G-code/retention-force proof. Current holder is a mechanical proposal and source dimensions/print tolerances must be checked on actual parts.

15 September motor carrier: MOTOR-CARRIER-01 v0.1, hardware/motor-carrier/README.md and cad/motor-carrier/README.md. Original TA6586 schematic and routed 30x18x1.6 PCB, 14 positions/27 pads (seven positions are integral solder pads, not purchased connectors). ERC/DRC/parity0, four pin-map oracle mutations. No physical/current qualification or fabrication release. Proposed upright Rubycon 470uF/16V max8.5x13 raises assembly height from old10 to16mm; C1 is underneath. Separate CAD proposal at0/+3 clears checked solids/cavity, old antenna route collides8.909mm3, revised route clears selected solids. Mounting, four other wire routes, wire-wire checks and all adjustment/service motions remain open. Current-layout0.6 remains historical geometry with driver placeholder. Do not transfer its27 acceptable pairs or service evidence to the new carrier. VM/DRIVE_GND do not authorize raw LW/charger ground connections. CadQuery minimal cube exits1 in this environment; direct OCP CAD generator exits0.

15 September servo/paired outputs: firmware commit68a6cca, sibling docs/servo-outputs.md. Explicit frequency and negative/neutral/positive pulse calibration, no default SG90 profile; SDK frequency mismatch is rejected. Servo native LOW_SPEED channel4/timer2/13bits, GPIO27 WROOM or D3/GPIO4 XIAO. ActuatorOutputs pairs motor/servo: first sample all off; valid zero-throttle steering works; inactive/stale plans disable pulses and traction; a servo write failure prevents new traction and latches shared fault. Pulse-disable is a proposal, not measured wheel centering or servo power-off. 8004 host positions/six mutations/four target builds passed; physical adapters still unused, main unchanged/null, no upload or actual calibration. Motor/servo write success is not feedback on motion. Preserve independent camera-only failure behavior.

15 September motor PWM candidate: see sibling RC_CAR_ESP32/docs/motor-pwm.md and docs/drive-output-plan.md. Actual IDF4.4.7 GPIO/LEDC adapter code is now compiled but unused; main remains NullMotorSink and no upload occurred. LOW_SPEED channels2/3 timer1, ten-bit period1024, explicit frequency (20kHz test fixture only), cap250permille. Zero uses ledc_stop(idle0); initialization/write errors latch and attempt both clears. Do not enable after builds alone: physical wiring/levels, startup/stop timing, PWM frequency, current limiting, camera coexistence and servo calibration remain open. GPIO latch failure must skip enabling that pin's output direction. The adapter requires exclusive channels/no fades; native IDF speed-group numbering differs from classic ESP32 Arduino channel numbering.

15 September motor dispatch: firmware commit d42e07b adds MotorDispatch -> NullMotorSink in the existing watchdog. Read docs/drive-output-plan.md and sibling firmware docs/motor-dispatch.md. Inactive channel is cleared first, both zero writes are attempted on failures, and sink faults latch; malformed/stale plans are rejected. Software mock tests/four injected mutations and four firmware builds passed. There is no GPIO/LEDC backend, upload or new device evidence; outputsEnabled=false. Do not interpret applied_to_sink as physical output. Parts in transit do not block further software, CAD or circuit preparation; physical fit, current, RF, FPV and stop measurements remain pending.

15 September current layout 0.6: cad/current-layout/viewer.html is the latest interactive scene; read its README. New 144-pair rigid sweep uses the current cradle/guides and union of 103 component boxes, not the old single XIAO envelope. Twenty-seven solid-fit pairs pass; all also clear the service USB allocation with body removed. Only 0/+3 has prior routed-wire/service evidence. Other poses hide five wires and explicitly require rerouting/service checks. Camera screw slot limits are checked separately; no continuous travel/physical clamp proof. Comparison with six old poses changes both geometry and envelope method, not measured real-world range. Source hashes and browser artifacts are in docs/evidence/current-*.

15 September harness guides: read cad/harness-guides/README.md. Two open saddles are integral to the tray and XIAO cap, requiring removable ties/lacing; no snap-fit/physical retention proof. New tray/cap replace the previous versions for this scene. Nominal 0/+3 geometry clears selected obstacles/103 component boxes, service USB and hypothetical shell. Updated body removal 76 poses, left guard25, cap28 and continuous 50mm battery-box exit pass. Before cap removal, remove screws, release tie and move servo wire out of the saddle; free-wire reach/strain is not modeled. Remaining wiring, all adjustments, physical fit, actual body and electrical operation remain open. Keep earlier geometry/evidence as historical.

15 September XIAO holder proposal: read cad/xiao-mount/README.md before using old camera access assumptions. Named STEP inspection identifies USB at +X (solid18) and U.FL left/lower (solid35); v0.5 service_usb at -X and its antenna endpoint were wrong. New cradle/cap, right stage screw Y77 instead of81, tray edge relief and three revised wire corridors are separate candidate files. Check against 103 component boxes and nominal packaging, not actual OV3660 revision or clamp strength. The old 0.5 viewer/geometry remains historical; do not transfer its adjustment/body-removal acceptance to the new holder. Prototype remains physically unassembled; parts in transit. Next mechanical work: harness fixation and rechecking whole-body removal/adjustment with the new holder.

15 September service audit: cad/packaging-service.md and tools/check_packaging_service.py recheck selected current 0.5 solids at nominal 0/+3 mm. The battery's continuous 50 mm -X box sweep is clear with body/band/LEFT guard removed; right guard can remain. Left guard sampled at 25 outward positions and body route +2Z,+1X,+65Z at 76 positions are clear for listed solids. Positive controls detect retained left guard and straight-up USB collision. Not a full physical service proof: battery leads/connectors, fingers, magnets and some original moving mechanism are omitted. wire-power.stl is not watertight after STL export; all five wire check solids are rebuilt from pinned radius/points rather than silently repairing display meshes. More CAD work remains possible before delivery; prioritize XIAO retention and harness fixation.

15 September current update: all five bench items are ORDERED AND IN TRANSIT, confirmed by the user: WROOM antenna, XIAO ESP32S3 Sense with OV3660, F130, SG90 and Waveshare DC5-36-TO-DC3V3-5-M. No arrivals are confirmed; exact purchased SKUs, quantities, prices and ETAs remain unknown. Do not ask again whether these were ordered or recommend duplicate purchases. Older no-order statements below are historical.

Read cad/packaging-v05/README.md and NOTICE.md for current CAD. Packaging 0.5 imports a pinned FreeCAD SG90 STEP, retains Seeed 2023 STEP and adds a drawing/photo-derived Waveshare -M approximation, separate electronics bays, wire corridors and charge-only USB geometry. Proposed local frame relief removes reference STEP interference (3.724 -> 0 mm3), not proof of actual servo fit or strength. Six of 144 sampled electronics/camera positions fit the checked bodies and hypothetical shell; nominal 0/+3 mm. Full slot travel is not qualified usable travel. Charger/driver are volume allocations; antenna/terminal dimensions are estimates. Old battery/body-removal or suspension evidence does not qualify the new assembly. User explicitly requests detailed CAD work while parts travel; missing hardware does not block it.

15 September: user reported one LW pack's white-connector readings with black/red probes respectively 1/2=-3.6 V, 2/3=-3.6 V, 1/3=-7.23 V. User-local wire colors: 1 red, 2 white, 3 black. User measurements establish pack+/midpoint/pack- for this sample. Read docs/battery-and-body.md and docs/evidence/lw-voltage-user-2026-09-15.json. User subsequently confirmed DCV20 and replacement of the meter battery before these readings; do not ask again. Accuracy remains unverified independently. Do not re-ask the three reported voltages or claim exact cell balance, pack health, charge rating or protected/raw-ground equivalence. User also chose one Waveshare DC5-36-TO-DC3V3-5-M for easier bench testing after the 7805 comparison; order is now confirmed; arrival and fleet adoption are not confirmed.

14 September: battery voltage checks are on hold because the user reports suspect multimeter readings: constant "1" at 20 V and "-349" at 200 V with leads disconnected from the meter. User identified the meter as DT838; brand/revision, decimal point and DC/AC setting remain unconfirmed. The common DT838 input is labelled VΩmA (combined voltage/current socket); use it with COM in DCV mode, never the separate 10A socket for voltage. Earlier 3.8-4.0 readings have no confirmed pin mapping and must not be recorded as validated cell voltages. Verify the meter separately from LW before resuming; a zero check alone does not establish accuracy. Do not infer a charge setting from these readings.

14 September user equipment update: Creality K1C with ABS/PLA/PETG; hot-air soldering station available, no hotplate. No WROOM antenna, no adjustable current-limited lab supply (only ordinary USB chargers), and no oscilloscope access. Do not ask these availability questions again. Battery model/label and purchase link are already supplied; do not request them again. Charger OUTPUT/model remain unknown, but do not repeat that request now. Current requested measurement is voltage only: disconnected pack white connector pairs 1-2, 2-3, 1-3 with consistent orientation, probe polarity and signed readings. No ammeter measurement is requested. Mechanical test-print preparation can proceed with the known printer/materials; a physical print is not confirmed. USB chargers do not establish the electrical bench current limit or transient measurement capability.

Read docs/first-prototype.md for the immediate physical path. Radio/FPV from USB can progress before finishing the custom charger once a WROOM antenna or actual XIAO Sense/OV3660 is available; arrival is unconfirmed. Read-only recheck14September04:29UTC found COM4 CP210x and Samsung/installed vea.raceremote, not RF or current flash proof. Existing failed antenna-less RF runs must not be repeated unchanged. Existing LW charger specifications remain unknown; see the narrower current voltage-only request in docs/first-prototype.md. Architecture-and-unknowns v0.3 now reflects the combined XIAO candidate and implemented Control v1, not the old two-board/stub description. More charger candidate sheets do not substitute for battery data, fabrication capability or a physical prototype; retain the final own-USB-C requirement.

PACK-BAL-01 v0.1 now has hardware/pack-balancer, a BQ29209 battery-side bench schematic for A research. 12 components/35 pins inclEP9; ERC0/five injected wiring faults/PDF checked. BAT_RAW_N must remain raw cell negative before low-side protection, never automatically USB_GND/CHG_GND/PACK_MINUS. J801 is logical emulator wiring, not LW pinout. J802 is battery-domain IO only: OUT can reach9.5V, is activeHIGH and not open drain; no direct MCU/FAULT_BUS_N. CB_EN pulls up to filtered battery voltage, defaults disabled; below6V enable policy and domain crossing remain absent. C802 goes VC2-to-VC1, not ground. 120ohm RCB is small balancing only; RCB1/2 nomenclature/internal resistance is ambiguous, so no precise current inferred. BQ29209 is secondary4.30V OVP, not4.20V regulation or complete BMS. A removes programmable1.5A reset risk but remains incomplete; no A/B winner, purchases, physical balance or full cost claim.

Read docs/charge-sense-paths.md before assigning INA300 J702 to BQ25887. CHARGE-SENSE-01 v0.1 derives a DC blind spot: with upper balancing, a BAT-to-pack-plus shunt reads only upper-cell current; lower current also includes the CBSET bypass. Example: 301.663mA sensed while lower cell gets400mA, below the proposed348mA detector threshold. A return-only shunt has the complementary blind spot. Two charge-lead measurements with ORed faults are a proposal, not a selected second INA300/PCB/BOM; charger return must not be bypassed by traction/debug/USB ground or battery protection. External shunts also bias cell-voltage readings. 378 DC combinations and eight examples verify equations only, no analog startup or physical charge. Existing OC v0.2 schematic and current priced subset still contain one channel. Keep A as reserve.

CHARGE-OC-01 v0.2 now has hardware/charge-overcurrent: INA300 ENABLE/LATCH hardwired AUX, HYS open, DELAY GND; no MCU clear/disable. ALERT shares FAULT_BUS_N/R401 with permit J402. Proposed two-terminal Yageo RL2512FK-070R1L shunt is0.1ohm/1%/1W at70C, TCR600ppm/C; J702 is a bench series path, NOT final battery/BQ BAT/MID/CBSET wiring. ERC0,23 pins/7 components,six injected faults/six ideal window scenarios. Held OC blocks repeated ARM even after current drops, but short repeated bursts may evade50us mode. No proof of power-cycle latch recovery, startup/brownout, charge peak/energy, shunt heat or all cell/balance paths. Do not short LATCH to ground: it is tied to AUX. R702 costs8 RUB with MOQ10/multiple5:80 RUB for1/4/6;9 local pieces do not prove MOQ10 immediately available locally. Current B priced subset3053/9082/12032; use generic B_latest_priced_subset_RUB going forward, retain older fields as historical subsets. No orders or physical charge.

CHARGE-MCU-01 v0.2 / CHARGE-IO-01 v0.1: hardware/charger-mcu contains a CH32V003F4P6 + SN74LVC3G14DCTR interface schematic (17 components/70 pins), not firmware/PCB. PD4 now reads PERMIT_Q; PA1 is FAULT_BUS_N open-drain plus input readback (never push-pull HIGH); PACK_SENSE/LED no longer assigned. PD5 ARM_N, PD6 WD_HEARTBEAT_N and PD0 SOURCE_REQUEST_N are open drain through inverters: released GPIO -> external LOW, sink -> HIGH. Preserve SWIO/PD1 and reserved reset/PD7. Permit v0.4 changes only R402 10k->47k for <=100uA buffer load. Three interfaces match fresh permit/watchdog/BC exports; seven injected faults/eight truth combinations passed. GPIO initialization, thresholds between tabulated VCC points, ramps, actual loading and total AUX remain unqualified. J604 SOURCE_REQUEST is not direct SOURCE_ALLOW; INA300 clear/disable is not connected to MCU. New U602 costs57/228/342 RUB for1/4/6; the51 RUB offer has MOQ11. Latest B priced subset with MCU IO is2973/9002/11952, incomplete; earlier regulated_CD field excludes U602. No orders.

CHARGE-WD-01 v0.1 now has a separate candidate schematic in hardware/charge-watchdog; read its README. TPS3431 WDO pin7 and ENOUT pin8 share FAULT_BUS_N to permit J402; no second pull-up. EN/SET1 are hardwired to AUX, CWD has 10k to AUX, WDI has 47k to ground. Factory timeout and fault pulse each170/200/230ms; a fault release must not automatically rearm. ERC0, 21 pins/7 components, five injected wiring faults and 90 settled-rail sequences passed. The model starts after initialization, not at power-on. New erroneous ARM edges can rearm, fast heartbeats can mask faulty firmware; this is not a complete independent current cutoff. GPIO margins, startup/brownout, combined bus loading, MCU/INA300/full PCB and physical charge remain open. TPS3431 was already priced in the common A/B subset: do not add its price twice. No fresh price/stock check or orders.

Android DRIVE-UI-01 v0.1 adds a driving layout with a large central video and two relative-drag pads; read docs/driving-ui.md. Setup/driving transitions clear inputs but preserve video and control connections. Each pad tracks its original finger; release/cancel/out-of-bounds clears only that axis and an already-held second finger must not take over. STOP and lifecycle disconnect clear/disable pads. Samsung evidence: 27 JVM tests, four device tests with loopback controllers/video; not ESP32, camera freshness/FPS or physical motor proof. Keep the independent control/video failure rule and authoritative firmware watchdog. Do not turn the synthetic screenshot into FPV acceptance.

hardware/charge-permit now contains CHARGE-PERMIT-01 v0.4, CHARGE-CD-01 v0.2 and CHARGE-BIAS-01 v0.1. Read its README and docs/charge-bias.md. U406 TPS70933DBVR creates a separate CHG_BIAS_3V3 (target 3.0..3.6V) for supervisor/TMUX; AUX still comes from U202. J401.3 is now CHG_SUPPLY_INPUT to the new LDO, not an external ready bias rail. EN/NC intentionally float per TPS709, do not join EN to IN or parallel the two 3.3V rails. This removes the direct 5.55V versus 5.5V rating mismatch: LDO input supports 30V, NOT the whole charger. Low headroom, startup/brownout, overshoot and full gate/core integration remain unqualified. Six ICs/23 components, ERC0, 37 IC pins/75 total, ten injected faults, 11 latch traces/18 settled CD cases. Assumed bias budget 2mA includes approx1mA preload, not a hardware limit. Six ICs cost386/1294/1776 RUB for1/4/6. Keep source permission separate from charge permission; MCU/watchdog/INA300 integration and physical charging remain unfinished. No orders.

Read docs/charge-current-guard.md (CHARGE-OC-01 v0.2): INA300 is only a candidate overcurrent DETECTOR, not a current limiter or a completed independent shutdown. Proposed 0.1ohm/1.74k/50us gives nominal 348mA; it is not an approved LW rating. 50us is the comparator setting, not a bound on full interruption or residual capacitor current. Need independent startup/brownout inhibit, fault latch/actuator, measured peak/duration and battery acceptance. INA300 ALERT is active-low while BQ25887 CD is active-high; do not connect directly. SNS pins 15/16 on BQ25887 are not remote battery sense inputs. Shunt placement can disturb cell measurements/balancing; CBSET may carry current outside a pack-plus shunt. Oscilloscope access was asked and is unanswered. INA300 costs 370 RUB for one or 120 RUB with MOQ5; adding IC alone leaves only 33 RUB priced-subset advantage for B on one prototype. A separate candidate schematic is now in hardware/charge-overcurrent; no PCB or physical protection proof.

Read docs/charge-node-comparison.md v0.2 and docs/charger-mcu.md before extending the charger. Both A/B now need a source MCU; CH32V003F4P6 TSSOP20 is the interface candidate (90 RUB snapshot; allocation and schematic v0.2, no firmware). Keep PD1 SWIO and PD7 reset reserved. Internal PDR 2.30..2.66V falling is below the AUX/MCU operating floor; software PVD is not an external brownout gate. 8MHz HSI 2.4mA is typical, not a maximum or whole AUX budget proof. Priced subsets favor BQ25887 but are incomplete; TPS3431 stock cannot cover 4/6. Next priority is BQ25887 independent reset/current protection before full schematic selection. B's 1.5A default/reset setting is not approved for LW; keep A as reserve. No fleet purchase or physical charge is qualified.

hardware/usb-detector now contains USB-BC-01 v0.1, a PI3USB9201 candidate interface. Read its README before integration: 7-bit I2C address 0x5F, client mode only, connector D+/D- are pins 8/7; device USB+/USB- pins 1/2 stay unused. ENB pull-up defaults the detector OFF; it is not the power gate. Client/host status clear on read, so events must belong to the current CC/VBUS attachment and reset must invalidate them. No MCU, firmware, CC, brownout/watchdog or SOURCE_ALLOW generator is implemented. ERC/12 pins/three injected faults passed, not physical detection. Since this candidate needs an autonomous MCU, compare complete charger A/B cost and size again; do not claim the BQ25886 system is MCU-free. PI3 sample offer is 380 RUB from one, 5-6 weeks; 130 RUB offer has MOQ 3000 and is excluded. No orders.

Read docs/usb-source-qualification.md for the charger input gap: BQ25886 ILIM cannot be set below 500 mA and its nominal 500 mA setting is not a strict 500 mA ceiling. PG does not expose source type and CE leaves SYS powered. BQ24392 and TUSB320LAI are researched candidates only. TUSB has internal Rd and cannot be attached unchanged to the current port's populated R1/R2; a separate coordinated BOM revision would be required. Never run two BC1.2 detectors on the same D+/D- lines concurrently. Source detection and current-budget qualification remain unimplemented; the auxiliary regulator has a schematic, not physical validation. Core v0.2 no longer shares USB data lines: R108 configures its detector locally, so no detector mux is needed. BQ24392 cannot distinguish DCP from Apple/TomTom by its outputs; CHG_DET HIGH is not permission for >=1.5A. Keep its purchase as research only until this is resolved.

hardware/usb-input-gate contains USB-INPUT-GATE-01 v0.2 and USB-AUX-01 v0.1: TPS25200DRVR gate plus TPS70933DBVR pre-gate 3.3V supply. AUX is now an OUTPUT on J203.1, not an external supply input: never parallel another regulator or XIAO. LDO EN intentionally floats per SBVS186H 7.4; do not tie it to VBUS or substitute TPS709A/B/TP pinouts/specs. Proposed AUX load <=10mA is not a hardware current limit. Source recognition, brownout and SOURCE_ALLOW generation remain external. Main RILIM 82.5k gives an equation-based 1.064..1.295A estimate, requiring a verified >=1.5A source. ERC, 12-pin/interface checks and six injected faults passed. No PCB or hardware. Conditional 5.55V clamp does not bound overshoot or close the core 5.5V contract; CE/STAT/PG absolute maximum is 6V. Do not merge GATED_VBUS/CHG_VBUS before integration review. Gate OFF removes USB input, not battery power from SYS; no battery/traction/service-USB backfeed.

The SG90 fit uncertainty is now localized in cad/servo-clearance/README.md: frame passage 22.8 mm, upstream reference rear face width 22.4 mm, selected seller envelope 23 mm. Four side interferences total 7.000 mm3; the 36.815 mm3 front-lower box interference may depend on actual shaft/case shape. Do not dismiss all interference as an envelope artifact or declare SG90 drop-in fit. The open upstream servo mesh is visualization only, not the supplier's CAD. No frame was cut. A 0.2 mm/side allowance is a proposal pending the physical sample/printer, not a measured tolerance.

Drive planning now exists in ../RC_CAR_ESP32/prototype/drive_plan.h, called by the independent watchdog task. Read docs/drive-output-plan.md. It is a shadow plan only: outputsEnabled stays false and actuator GPIOs are not configured. Proposed cap 250 permille, rise 1000 permille/s and reverse pause 200 ms are not measured motor limits. Preserve the STOP revision/session barrier and independent freshness check when adding a hardware backend. Steering is normalized, not calibrated microseconds. Host checks, three injected faults and four affected builds passed. Subsequent c9fabb8 upload/flash verification and Samsung device tests are in docs/drive-device-bench.md. Forward/reverse/neutral shadow states and silent-command watchdog were observed, with Serial confirming timeout in attempt 3. All three native full scenarios failed; do not claim reliable control or explicit final STOP verification. No physical motion. DriveDeviceBenchTest requires explicit disabled-output opt-in and skips by default.

A fixed six-session neutral comparison also failed both with and without periodic HTTP (three trials each). See the comparison section in docs/drive-device-bench.md. HTTP polling is not the sole cause; root cause remains open. Preserve the 200/250 ms stops and use an antenna for the next RF comparison. Do not repeat this unchanged setup until a lucky pass.

Delivery city: Cheboksary, explicitly confirmed by the user. Do not ask for the city again; shipping method and a specific pickup point/address remain unselected.

Preferred purchasing sources: Ozon, Wildberries, AliExpress, and Chip and Dip (chipdip.ru). Search these first. Other shops in older price tables are research references or fallback candidates, not accepted sellers. Verify the exact variant, seller, price conditions and shipping when available. AliExpress challenged the automation browser even when the user tried solving its CAPTCHA; avoid repeated challenges and do not claim the technical cause is known. Mark inaccessible offer details unverified and continue other research.

ChipDip internal browser search (`/search?searchtext=...`) exposed offers missing from general web search. Use it before concluding that a part is unavailable. Inspect the product card for minimum order and quantity prices: a low search-result unit price can require buying a whole lot. Record required quantities separately from minimum purchase quantities/costs; see docs/charge-parts.md.

Before working on this project, read `docs/project-brief.md` and `docs/contracts.md`. For chassis work, also read `docs/chassis-candidates.md`.

AliExpress fallback verified on Windows: the user's ordinary Chrome product tab can expose its loaded text through UIAutomationClient AutomationElement (window handle) -> Document -> TextPattern.DocumentRange.GetText. This worked where curl and the separate MCP browser received CAPTCHA. Locate the current product window and document each time; scope reads to the product document and the actual address-bar control, since arbitrary Edit controls can contain unrelated drafts. The MCP browser is separate and list_pages can recreate it after the user closes it. See docs/aliexpress-shortlist.md for the observed SKU and conditional OV2640/OV3660 shipment.

The user explicitly requires recording key points and contracts as work progresses:

- Record accepted requirements, decisions, constraints, significant findings and unresolved questions in the project documents during the same task.
- After changing repository files, verify the changes and create a Git commit before finishing the task. This is an explicit user requirement and includes documentation changes. Commit only files belonging to the task; do not include unrelated user changes. Creating the commit does not require asking again. Pushing is a separate action.
- Keep user decisions separate from agent proposals and unverified assumptions. Do not silently promote a candidate component or suggested value to an agreed contract.
- For each interface contract, record its status, participants, units/ranges, failure behavior, version and verification evidence when known. Leave unknown values explicitly open.
- When a decision changes, update the canonical description and briefly record what changed and why. Keep source links and tested revisions for externally derived facts.
- Do not claim hardware compatibility, reliability or performance based only on a code review or successful software build.

The fleet must use one repeatable target configuration, including the first target prototype. The user now explicitly requires designing around the three existing LW LiPo 601844HP batteries (7.4 V, 600 mAh, 20C), with quick battery replacement. This supersedes the earlier generic preference against reusing inventory for batteries; other existing hardware remains experimental inventory. Camera is optional and bodies are customizable.

The model-car body must constrain component packaging from the start and give the car a finished appearance. A generic CAD shell is a proposed envelope, not evidence that a purchasable Mini-Z body fits. Read cad/body-study.md for the current alternative and its unverified electronics assumptions. In the aligned zcar scene front is +Y (steered axle Y=111.125 mm), rear driven axle Y=21.125 mm.

The user proposed longitudinally adjustable electronics to accommodate different cabin positions. Continue with cad/adjustable-layout.md (0.3): separate adjustable power-electronics tray and camera mount. The nominal 11 mm range, screw details and hypothetical shell are agent proposals with discrete CAD checks, not validated physical compatibility or an unlimited Mini-Z body interface.

Confirmed scope: indoor home racing on a flat surface, Android and iPhone. On loss of control commands, stop the car. Camera-only failure must preserve otherwise working control; do not add a video-dependent driving interlock. KMP is the proposed application direction, not an already completed migration.

The cars are intended as finished gifts for friends. Each must charge through its own USB-C port from an ordinary available charger; do not substitute a shared hobby charger for this product requirement. The user already has a charger (model unknown). A baseline 5 V USB charging design without mandatory PD/QC is the proposed implementation; the built-in 2S charger and USB/power integration remain unfinished.

The user permits a separate charge-only USB-C and additional electronics when needed for a well-finished product; a shared charge/data port is no longer required. Prioritize easy cable insertion/removal and the ability to hide or disguise the port. Rear placement and exhaust styling are examples, not mandatory geometry. The current agent proposal is a chassis-mounted charging port with a removable cosmetic cover and the native XIAO service USB under the body. See docs/usb-c-power.md; the mount has CAD evidence only; the full charger remains unfinished.

The side-port CAD proposal is in cad/usb-port-study.md (USB-MOUNT-01 v0.4). The passive port PCB is now routed in hardware/usb-port: 26×7.6×1 mm, USB4105-GF-A, two independent 5.1k Rd, six harness terminals. KiCad 10.0.6 ERC, DRC and schematic parity passed; tools/verify_usb_port.py compares 27 logical pins and detects swapped CC / missing Rd in mutated copies. The native schematic and PDF are in hardware/usb-port. Physical assembly and the separate 2S charger remain unfinished. Pad/hole geometry drives the updated CAD, connector face X=59.1 mm. Use the KiCad Python in build/tooling/kicad-10.0.6 to regenerate; setup/evidence is in hardware/usb-port/README.md. Keep the charger separate; never add another Rd pair on the main board, preserve quick battery extraction and adjustable electronics.

USB-MOUNT-01 v0.4 adds a removable magnetic lid with guide rim, three stops and a finger lip. Its surround belongs to the hypothetical printed body, while the PCB stays on the chassis. Four 3x1 mm magnets are proposed, not mechanically validated. Print-oriented lid/pocket/guide coupons and 33 sampled lid-removal checks are in cad/usb-port-study.md. Body removal cannot be straight up: the checked neutral-stage route is +2 mm Z, +1 mm X, then lift to +65 mm Z (76 poses). Real body mounts must allow this route or geometry must change. User confirmed Creality K1C and ABS, PLA, PETG on 14 September. Printer/material are no longer unknown; nozzle and actual slicer settings are unverified. Do not claim slicing or printing without evidence. Harness retention and physical cable/force checks remain open.

The BQ25886 converter-core schematic is in hardware/charge-core (CHARGE-CORE-01 v0.2): R108=0 ohm joins CORE_DP/CORE_DM locally; J101 is now TWO-PIN power only. Physical USB D+/D- must stay with the external detector and never reach these core nets. TI E2E explicitly supports the local DCP setting; it does not qualify the real source or grant 3A. RILIM remains 1.37k. ERC, 25-pin/interface/isolation checks and four injected faults passed; PDF/PNG updated. Physical local DCP/ICO behavior is not tested. This is only the converter core, not a complete charger or PCB. Source qualification, cell protection/balancing, external charge permission and physical tests remain open. Nominal 250mA is an agent bench proposal, not an approved LW rating. CE disables charge but leaves SYS powered; do not use it as the traction power interlock. Core connectors are logical interfaces, not the battery's measured pinout; status/pull-ups reach USB voltage and are not direct 3.3V GPIO signals. User confirmed a hot-air soldering station on 14 September; no hotplate. Consider hot-air assembly for the prototype, but assembly process and joint quality remain unverified.

Application identity: use `vea.raceremote` for applicationId, namespace, Kotlin packages, tests and future platform identifiers. First target is Android; iOS can be stubbed/deferred. Samsung SM-G973F / Android 12 and ESP32 on COM4 are available. Soldering iron, multimeter and 3D printer are confirmed. The user confirmed the WROOM-32U external antenna is NOT connected and explicitly requested a nearby-phone-hotspot experiment without it. That experiment is authorized; do not demand an antenna before executing it. It connected but still timed out; repeat reliability measurements with a suitable antenna. Read docs/control-bench.md and docs/control-v1.md for implementation and evidence.
