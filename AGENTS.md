# Project continuity

14 September user equipment update: Creality K1C with ABS/PLA/PETG; hot-air soldering station available, no hotplate. No WROOM antenna, no adjustable current-limited lab supply (only ordinary USB chargers), and no oscilloscope access. Do not ask these availability questions again. Battery model/label and purchase link are already supplied; do not request them again. Charger OUTPUT/model remain unknown, but do not repeat that request now. Current requested measurement is voltage only: disconnected pack white connector pairs 1-2, 2-3, 1-3 with consistent orientation, probe polarity and signed readings. No ammeter measurement is requested. Mechanical test-print preparation can proceed with the known printer/materials; a physical print is not confirmed. USB chargers do not establish the electrical bench current limit or transient measurement capability.

Read docs/first-prototype.md for the immediate physical path. Radio/FPV from USB can progress before finishing the custom charger once a WROOM antenna or actual XIAO Sense/OV3660 is available; arrival is unconfirmed. Read-only recheck14September04:29UTC found COM4 CP210x and Samsung/installed vea.raceremote, not RF or current flash proof. Existing failed antenna-less RF runs must not be repeated unchanged. Existing LW charger specifications remain unknown; see the narrower current voltage-only request in docs/first-prototype.md. Architecture-and-unknowns v0.3 now reflects the combined XIAO candidate and implemented Control v1, not the old two-board/stub description. More charger candidate sheets do not substitute for battery data, fabrication capability or a physical prototype; retain the final own-USB-C requirement.

PACK-BAL-01 v0.1 now has hardware/pack-balancer, a BQ29209 battery-side bench schematic for A research. 12 components/35 pins inclEP9; ERC0/five injected wiring faults/PDF checked. BAT_RAW_N must remain raw cell negative before low-side protection, never automatically USB_GND/CHG_GND/PACK_MINUS. J801 is logical emulator wiring, not LW pinout. J802 is battery-domain IO only: OUT can reach9.5V, is activeHIGH and not open drain; no direct MCU/FAULT_BUS_N. CB_EN pulls up to filtered battery voltage, defaults disabled; below6V enable policy and domain crossing remain absent. C802 goes VC2-to-VC1, not ground. 120ohm RCB is small balancing only; RCB1/2 nomenclature/internal resistance is ambiguous, so no precise current inferred. BQ29209 is secondary4.30V OVP, not4.20V regulation or complete BMS. A removes programmable1.5A reset risk but remains incomplete; no A/B winner, purchases, physical balance or full cost claim.

Read docs/charge-sense-paths.md before assigning INA300 J702 to BQ25887. CHARGE-SENSE-01 v0.1 derives a DC blind spot: with upper balancing, a BAT-to-pack-plus shunt reads only upper-cell current; lower current also includes the CBSET bypass. Example: 301.663mA sensed while lower cell gets400mA, below the proposed348mA detector threshold. A return-only shunt has the complementary blind spot. Two charge-lead measurements with ORed faults are a proposal, not a selected second INA300/PCB/BOM; charger return must not be bypassed by traction/debug/USB ground or battery protection. External shunts also bias cell-voltage readings. 378 DC combinations and eight examples verify equations only, no analog startup or physical charge. Existing OC v0.2 schematic and current priced subset still contain one channel. Keep A as reserve.

CHARGE-OC-01 v0.2 now has hardware/charge-overcurrent: INA300 ENABLE/LATCH hardwired AUX, HYS open, DELAY GND; no MCU clear/disable. ALERT shares FAULT_BUS_N/R401 with permit J402. Proposed two-terminal Yageo RL2512FK-070R1L shunt is0.1ohm/1%/1W at70C, TCR600ppm/C; J702 is a bench series path, NOT final battery/BQ BAT/MID/CBSET wiring. ERC0,23 pins/7 components,six injected faults/six ideal window scenarios. Held OC blocks repeated ARM even after current drops, but short repeated bursts may evade50us mode. No proof of power-cycle latch recovery, startup/brownout, charge peak/energy, shunt heat or all cell/balance paths. Do not short LATCH to ground: it is tied to AUX. R702 costs8 RUB with MOQ10/multiple5:80 RUB for1/4/6;9 local pieces do not prove MOQ10 immediately available locally. Current B priced subset3053/9082/12032; use generic B_latest_priced_subset_RUB going forward, retain older fields as historical subsets. No orders or physical charge.

CHARGE-MCU-01 v0.2 / CHARGE-IO-01 v0.1: hardware/charger-mcu contains a CH32V003F4P6 + SN74LVC3G14DCTR interface schematic (17 components/70 pins), not firmware/PCB. PD4 now reads PERMIT_Q; PA1 is FAULT_BUS_N open-drain plus input readback (never push-pull HIGH); PACK_SENSE/LED no longer assigned. PD5 ARM_N, PD6 WD_HEARTBEAT_N and PD0 SOURCE_REQUEST_N are open drain through inverters: released GPIO -> external LOW, sink -> HIGH. Preserve SWIO/PD1 and reserved reset/PD7. Permit v0.4 changes only R402 10k->47k for <=100uA buffer load. Three interfaces match fresh permit/watchdog/BC exports; seven injected faults/eight truth combinations passed. GPIO initialization, thresholds between tabulated VCC points, ramps, actual loading and total AUX remain unqualified. J604 SOURCE_REQUEST is not direct SOURCE_ALLOW; INA300 clear/disable is not connected to MCU. New U602 costs57/228/342 RUB for1/4/6; the51 RUB offer has MOQ11. Latest B priced subset with MCU IO is2973/9002/11952, incomplete; earlier regulated_CD field excludes U602. No orders.

CHARGE-WD-01 v0.1 now has a separate candidate schematic in hardware/charge-watchdog; read its README. TPS3431 WDO pin7 and ENOUT pin8 share FAULT_BUS_N to permit J402; no second pull-up. EN/SET1 are hardwired to AUX, CWD has 10k to AUX, WDI has 47k to ground. Factory timeout and fault pulse each170/200/230ms; a fault release must not automatically rearm. ERC0, 21 pins/7 components, five injected wiring faults and 90 settled-rail sequences passed. The model starts after initialization, not at power-on. New erroneous ARM edges can rearm, fast heartbeats can mask faulty firmware; this is not a complete independent current cutoff. GPIO margins, startup/brownout, combined bus loading, MCU/INA300/full PCB and physical charge remain open. TPS3431 was already priced in the common A/B subset: do not add its price twice. No fresh price/stock check or orders.

Android DRIVE-UI-01 v0.1 adds a driving layout with a large central video and two relative-drag pads; read docs/driving-ui.md. Setup/driving transitions clear inputs but preserve video and control connections. Each pad tracks its original finger; release/cancel/out-of-bounds clears only that axis and an already-held second finger must not take over. STOP and lifecycle disconnect clear/disable pads. Samsung evidence: 27 JVM tests, four device tests with loopback controllers/video; not ESP32, camera freshness/FPS or physical motor proof. Keep the independent control/video failure rule and authoritative firmware watchdog. Do not turn the synthetic screenshot into FPV acceptance.

hardware/charge-permit now contains CHARGE-PERMIT-01 v0.4, CHARGE-CD-01 v0.2 and CHARGE-BIAS-01 v0.1. Read its README and docs/charge-bias.md. U406 TPS70933DBVR creates a separate CHG_BIAS_3V3 (target 3.0..3.6V) for supervisor/TMUX; AUX still comes from U202. J401.3 is now CHG_SUPPLY_INPUT to the new LDO, not an external ready bias rail. EN/NC intentionally float per TPS709, do not join EN to IN or parallel the two 3.3V rails. This removes the direct 5.55V versus 5.5V rating mismatch: LDO input supports 30V, NOT the whole charger. Low headroom, startup/brownout, overshoot and full gate/core integration remain unqualified. Six ICs/23 components, ERC0, 37 IC pins/75 total, ten injected faults, 11 latch traces/18 settled CD cases. Assumed bias budget 2mA includes approx1mA preload, not a hardware limit. Six ICs cost386/1294/1776 RUB for1/4/6. Keep source permission separate from charge permission; MCU/watchdog/INA300 integration and physical charging remain unfinished. No orders.

Read docs/charge-current-guard.md (CHARGE-OC-01 v0.2): INA300 is only a candidate overcurrent DETECTOR, not a current limiter or a completed independent shutdown. Proposed 0.1ohm/1.74k/50us gives nominal 348mA; it is not an approved LW rating. 50us is the comparator setting, not a bound on full interruption or residual capacitor current. Need independent startup/brownout inhibit, fault latch/actuator, measured peak/duration and battery acceptance. INA300 ALERT is active-low while BQ25887 CD is active-high; do not connect directly. SNS pins 15/16 on BQ25887 are not remote battery sense inputs. Shunt placement can disturb cell measurements/balancing; CBSET may carry current outside a pack-plus shunt. Oscilloscope access was asked and is unanswered. INA300 costs 370 RUB for one or 120 RUB with MOQ5; adding IC alone leaves only 33 RUB priced-subset advantage for B on one prototype. A separate candidate schematic is now in hardware/charge-overcurrent; no PCB or physical protection proof.

Read docs/charge-node-comparison.md v0.2 and docs/charger-mcu.md before extending the charger. Both A/B now need a source MCU; CH32V003F4P6 TSSOP20 is the interface candidate (90 RUB snapshot; allocation and schematic v0.2, no firmware). Keep PD1 SWIO and PD7 reset reserved. Internal PDR 2.30..2.66V falling is below the AUX/MCU operating floor; software PVD is not an external brownout gate. 8MHz HSI 2.4mA is typical, not a maximum or whole AUX budget proof. Priced subsets favor BQ25887 but are incomplete; TPS3431 stock cannot cover 4/6. Next priority is BQ25887 independent reset/current protection before full schematic selection. B's 1.5A default/reset setting is not approved for LW; keep A as reserve. No fleet purchase or physical charge is qualified.

hardware/usb-detector now contains USB-BC-01 v0.1, a PI3USB9201 candidate interface. Read its README before integration: 7-bit I2C address 0x5F, client mode only, connector D+/D- are pins 8/7; device USB+/USB- pins 1/2 stay unused. ENB pull-up defaults the detector OFF; it is not the power gate. Client/host status clear on read, so events must belong to the current CC/VBUS attachment and reset must invalidate them. No MCU, firmware, CC, brownout/watchdog or SOURCE_ALLOW generator is implemented. ERC/12 pins/three injected faults passed, not physical detection. Since this candidate needs an autonomous MCU, compare complete charger A/B cost and size again; do not claim the BQ25886 system is MCU-free. PI3 sample offer is 380 RUB from one, 5-6 weeks; 130 RUB offer has MOQ 3000 and is excluded. No orders.

Read docs/usb-source-qualification.md for the charger input gap: BQ25886 ILIM cannot be set below 500 mA and its nominal 500 mA setting is not a strict 500 mA ceiling. PG does not expose source type and CE leaves SYS powered. BQ24392 and TUSB320LAI are researched candidates only. TUSB has internal Rd and cannot be attached unchanged to the current port's populated R1/R2; a separate coordinated BOM revision would be required. Never run two BC1.2 detectors on the same D+/D- lines concurrently. Source detection and current-budget qualification remain unimplemented; the auxiliary regulator has a schematic, not physical validation. Core v0.2 no longer shares USB data lines: R108 configures its detector locally, so no detector mux is needed. BQ24392 cannot distinguish DCP from Apple/TomTom by its outputs; CHG_DET HIGH is not permission for >=1.5A. Keep its purchase as research only until this is resolved.

hardware/usb-input-gate contains USB-INPUT-GATE-01 v0.2 and USB-AUX-01 v0.1: TPS25200DRVR gate plus TPS70933DBVR pre-gate 3.3V supply. AUX is now an OUTPUT on J203.1, not an external supply input: never parallel another regulator or XIAO. LDO EN intentionally floats per SBVS186H 7.4; do not tie it to VBUS or substitute TPS709A/B/TP pinouts/specs. Proposed AUX load <=10mA is not a hardware current limit. Source recognition, brownout and SOURCE_ALLOW generation remain external. Main RILIM 82.5k gives an equation-based 1.064..1.295A estimate, requiring a verified >=1.5A source. ERC, 12-pin/interface checks and six injected faults passed. No PCB or hardware. Conditional 5.55V clamp does not bound overshoot or close the core 5.5V contract; CE/STAT/PG absolute maximum is 6V. Do not merge GATED_VBUS/CHG_VBUS before integration review. Gate OFF removes USB input, not battery power from SYS; no battery/traction/service-USB backfeed.

The SG90 fit uncertainty is now localized in cad/servo-clearance/README.md: frame passage 22.8 mm, upstream reference rear face width 22.4 mm, selected seller envelope 23 mm. Four side interferences total 7.000 mm3; the 36.815 mm3 front-lower box interference may depend on actual shaft/case shape. Do not dismiss all interference as an envelope artifact or declare SG90 drop-in fit. The open upstream servo mesh is visualization only, not the supplier's CAD. No frame was cut. A 0.2 mm/side allowance is a proposal pending the physical sample/printer, not a measured tolerance.

Drive planning now exists in ../RC_CAR_ESP32/prototype/drive_plan.h, called by the independent watchdog task. Read docs/drive-output-plan.md. It is a shadow plan only: outputsEnabled stays false and actuator GPIOs are not configured. Proposed cap 250 permille, rise 1000 permille/s and reverse pause 200 ms are not measured motor limits. Preserve the STOP revision/session barrier and independent freshness check when adding a hardware backend. Steering is normalized, not calibrated microseconds. Host checks, three injected faults and four affected builds passed. Subsequent c9fabb8 upload/flash verification and Samsung device tests are in docs/drive-device-bench.md. Forward/reverse/neutral shadow states and silent-command watchdog were observed, with Serial confirming timeout in attempt 3. All three native full scenarios failed; do not claim reliable control or explicit final STOP verification. No physical motion. DriveDeviceBenchTest requires explicit disabled-output opt-in and skips by default.

A fixed six-session neutral comparison also failed both with and without periodic HTTP (three trials each). See the comparison section in docs/drive-device-bench.md. HTTP polling is not the sole cause; root cause remains open. Preserve the 200/250 ms stops and use an antenna for the next RF comparison. Do not repeat this unchanged setup until a lucky pass.

Delivery city: Cheboksary, explicitly confirmed by the user. Do not ask for the city again; shipping method and a specific pickup point/address remain unselected.

Preferred purchasing sources: Ozon, Wildberries, AliExpress, and Chip and Dip (chipdip.ru). Search these first. Other shops in older price tables are research references or fallback candidates, not accepted sellers. Verify the exact variant, seller, price conditions and shipping when available. AliExpress challenged the automation browser even when the user tried solving its CAPTCHA; avoid repeated challenges and do not claim the technical cause is known. Mark inaccessible offer details unverified and continue other research.

ChipDip internal browser search (`/search?searchtext=...`) exposed offers missing from general web search. Use it before concluding that a part is unavailable. Inspect the product card for minimum order and quantity prices: a low search-result unit price can require buying a whole lot. Record required quantities separately from minimum purchase quantities/costs; see docs/charge-parts.md.

Before working on this project, read `docs/project-brief.md` and `docs/contracts.md`. For chassis work, also read `docs/chassis-candidates.md`.

AliExpress fallback verified on Windows: the user's ordinary Chrome product tab can expose its loaded text through UIAutomationClient AutomationElement (window handle) -> Document -> TextPattern.DocumentRange.GetText. This worked where curl and the separate MCP browser received CAPTCHA. Locate the current product window and document each time; scope reads to the product document and the actual address-bar control, since arbitrary Edit controls can contain unrelated drafts. The MCP browser is separate and list_pages can recreate it after the user closes it. See docs/aliexpress-shortlist.md for the observed SKU and conditional OV2640/OV3660 shipment.

The user explicitly requires recording key points and contracts as work progresses:

- Record accepted requirements, decisions, constraints, significant findings and unresolved questions in the project documents during the same task.
- After changing repository files, verify the changes and create a Git commit before finishing the task. This is an explicit user requirement and includes documentation changes. Commit only files belonging to the task; do not include unrelated user changes. Creating the commit does not require asking again. Pushing is a separate action.
- Keep user decisions separate from agent proposals and unverified assumptions. Do not silently promote a candidate component or suggested value to an agreed contract.
- For each interface contract, record its status, participants, units/ranges, failure behavior, version and verification evidence when known. Leave unknown values explicitly open.
- When a decision changes, update the canonical description and briefly record what changed and why. Keep source links and tested revisions for externally derived facts.
- Do not claim hardware compatibility, reliability or performance based only on a code review or successful software build.

The fleet must use one repeatable target configuration, including the first target prototype. The user now explicitly requires designing around the three existing LW LiPo 601844HP batteries (7.4 V, 600 mAh, 20C), with quick battery replacement. This supersedes the earlier generic preference against reusing inventory for batteries; other existing hardware remains experimental inventory. Camera is optional and bodies are customizable.

The model-car body must constrain component packaging from the start and give the car a finished appearance. A generic CAD shell is a proposed envelope, not evidence that a purchasable Mini-Z body fits. Read cad/body-study.md for the current alternative and its unverified electronics assumptions. In the aligned zcar scene front is +Y (steered axle Y=111.125 mm), rear driven axle Y=21.125 mm.

The user proposed longitudinally adjustable electronics to accommodate different cabin positions. Continue with cad/adjustable-layout.md (0.3): separate adjustable power-electronics tray and camera mount. The nominal 11 mm range, screw details and hypothetical shell are agent proposals with discrete CAD checks, not validated physical compatibility or an unlimited Mini-Z body interface.

Confirmed scope: indoor home racing on a flat surface, Android and iPhone. On loss of control commands, stop the car. Camera-only failure must preserve otherwise working control; do not add a video-dependent driving interlock. KMP is the proposed application direction, not an already completed migration.

The cars are intended as finished gifts for friends. Each must charge through its own USB-C port from an ordinary available charger; do not substitute a shared hobby charger for this product requirement. The user already has a charger (model unknown). A baseline 5 V USB charging design without mandatory PD/QC is the proposed implementation; the built-in 2S charger and USB/power integration remain unfinished.

The user permits a separate charge-only USB-C and additional electronics when needed for a well-finished product; a shared charge/data port is no longer required. Prioritize easy cable insertion/removal and the ability to hide or disguise the port. Rear placement and exhaust styling are examples, not mandatory geometry. The current agent proposal is a chassis-mounted charging port with a removable cosmetic cover and the native XIAO service USB under the body. See docs/usb-c-power.md; the mount has CAD evidence only; the full charger remains unfinished.

The side-port CAD proposal is in cad/usb-port-study.md (USB-MOUNT-01 v0.4). The passive port PCB is now routed in hardware/usb-port: 26×7.6×1 mm, USB4105-GF-A, two independent 5.1k Rd, six harness terminals. KiCad 10.0.6 ERC, DRC and schematic parity passed; tools/verify_usb_port.py compares 27 logical pins and detects swapped CC / missing Rd in mutated copies. The native schematic and PDF are in hardware/usb-port. Physical assembly and the separate 2S charger remain unfinished. Pad/hole geometry drives the updated CAD, connector face X=59.1 mm. Use the KiCad Python in build/tooling/kicad-10.0.6 to regenerate; setup/evidence is in hardware/usb-port/README.md. Keep the charger separate; never add another Rd pair on the main board, preserve quick battery extraction and adjustable electronics.

USB-MOUNT-01 v0.4 adds a removable magnetic lid with guide rim, three stops and a finger lip. Its surround belongs to the hypothetical printed body, while the PCB stays on the chassis. Four 3x1 mm magnets are proposed, not mechanically validated. Print-oriented lid/pocket/guide coupons and 33 sampled lid-removal checks are in cad/usb-port-study.md. Body removal cannot be straight up: the checked neutral-stage route is +2 mm Z, +1 mm X, then lift to +65 mm Z (76 poses). Real body mounts must allow this route or geometry must change. User confirmed Creality K1C and ABS, PLA, PETG on 14 September. Printer/material are no longer unknown; nozzle and actual slicer settings are unverified. Do not claim slicing or printing without evidence. Harness retention and physical cable/force checks remain open.

The BQ25886 converter-core schematic is in hardware/charge-core (CHARGE-CORE-01 v0.2): R108=0 ohm joins CORE_DP/CORE_DM locally; J101 is now TWO-PIN power only. Physical USB D+/D- must stay with the external detector and never reach these core nets. TI E2E explicitly supports the local DCP setting; it does not qualify the real source or grant 3A. RILIM remains 1.37k. ERC, 25-pin/interface/isolation checks and four injected faults passed; PDF/PNG updated. Physical local DCP/ICO behavior is not tested. This is only the converter core, not a complete charger or PCB. Source qualification, cell protection/balancing, external charge permission and physical tests remain open. Nominal 250mA is an agent bench proposal, not an approved LW rating. CE disables charge but leaves SYS powered; do not use it as the traction power interlock. Core connectors are logical interfaces, not the battery's measured pinout; status/pull-ups reach USB voltage and are not direct 3.3V GPIO signals. User confirmed a hot-air soldering station on 14 September; no hotplate. Consider hot-air assembly for the prototype, but assembly process and joint quality remain unverified.

Application identity: use `vea.raceremote` for applicationId, namespace, Kotlin packages, tests and future platform identifiers. First target is Android; iOS can be stubbed/deferred. Samsung SM-G973F / Android 12 and ESP32 on COM4 are available. Soldering iron, multimeter and 3D printer are confirmed. The user confirmed the WROOM-32U external antenna is NOT connected and explicitly requested a nearby-phone-hotspot experiment without it. That experiment is authorized; do not demand an antenna before executing it. It connected but still timed out; repeat reliability measurements with a suitable antenna. Read docs/control-bench.md and docs/control-v1.md for implementation and evidence.
